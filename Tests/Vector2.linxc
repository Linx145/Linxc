#include <stdio.h>
#include <Linxc.h>

struct Vector2
{
    float x;
    float y;

    bool Equals(Vector2 other)
    {
        return x == other.x && y == other.y;
    }
    float Dot(Vector2 other)
    {
        return x * other.x + y * other.y;
    }
    void Print()
    {
        printf("%d, %d\n", x, y);
    }
};

struct trait_ToString
{
    void Print()
    {

    }
};