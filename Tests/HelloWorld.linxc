#include <stdint.h>
#include <stdio.h>

struct Vector2
{
    float x;
    float y;

    bool Equals(Vector2 other)
    {
        if (x == other.x && y == other.y)
        {
            return true;
        }
        else
        {
            printf("sex");
            return false;
        }
    }
};