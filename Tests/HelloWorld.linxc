#include <Linxc.h>

namespace MyNamespace
{
    struct MyStruct
    {
        i32 A;
        float B;
        bool C;

        void DoStuff(i32 amount, float mult)
        {
            if (amount > 5 || mult < 2.5f)
            {
                B += amount * mult;
            }
            else
                B += (float)amount;
        }
    };
    MyStruct NewStructure()
    {
        MyStruct result;
        result.A = 32;
        result.B = 16.5f;
        result.C = true;
        return result;
    }
}
i32 Main()
{
    //MyNamespace::MyStruct variable = MyNamespace::NewStructure();

    return 0;
}