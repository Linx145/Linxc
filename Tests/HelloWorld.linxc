#include <stdint.h>
#include <stdio.h>

struct Vector2
{
    float x;
    float y;

    bool Equals(Vector2 other)
    {
        if (x == other.x && y == other.y)
        {
            return true;
        }
        else
        {
            for (int i = 0; i < 5; i += 1)
            {
                x += 1;
            }
            return false;
        }
    }
};