#include <Linxc.h>
#include <stdio.h>

template<typename T>
struct Array
{
    T *ptr;
    u32 length;
};

template<typename T>
void PrintArrayLength(Array<T> array)
{
    printf("%u\n", array.length);
}